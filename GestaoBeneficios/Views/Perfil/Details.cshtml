@model GestaoBeneficios.DTO.PerfilDTO

@{
    ViewData["Title"] = "Perfis";
}

<h2>Detalhes</h2>

<div>
    <h4>Perfil</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Id)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Tipo)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Tipo)
        </dd>
        <dt>
        </dt>
        @if (Model.Tipo == "Administrador")
        {
        <dd>
            Permissão para interagir com todas as funções do sistema. <br />
            Recomendado para gerentes e gestores de RH.
        </dd>
        }
        else if (Model.Tipo == "Colaborador")
        {
        <dd>
            Permissão para interagir apenas com seus próprios dados. <br />
            Recomendado para os demais funcionários da empresa.
        </dd>
        }

        </dl>
</div>
<div>
    <a asp-action="Index">Voltar para a listagem</a>
</div>
